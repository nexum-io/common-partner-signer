{
  "name": "partner-signer-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "MCP stdio server for agents: getAddress / signTypedData / signMessage over @nexum-io/partner-signer (local partner EOA, key from the host process env)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ci:check": "npm run typecheck && npm run build && npm run test",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@nexum-io/partner-signer": "file:..",
    "viem": "^2.56.5",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  }
}
